{
  "name": "cosmolabe-monorepo",
  "version": "0.0.0",
  "private": true,
  "license": "Apache-2.0",
  "description": "Merged monorepo: Cosmolabe (product, visualization) and Bessel (compute engines, SDK, bessel CLI). See CLAUDE.md.",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "install:all": "pnpm -C bessel install --frozen-lockfile && npm --prefix cosmolabe ci",
    "fetch-kernels": "bash scripts/fetch-kernels",
    "verify:purity": "node scripts/purity-lint.mjs",
    "verify:goldens": "node scripts/golden-immutability.mjs",
    "verify:bessel": "pnpm -C bessel verify",
    "verify:cosmolabe": "npm --prefix cosmolabe run build && TZ=America/Los_Angeles npm --prefix cosmolabe run test -- --exclude '**/catalog-all-cosmographia.test.ts' --exclude '**/xyzv-parser.test.ts'",
    "verify:seam": "TZ=America/Los_Angeles node scripts/seam.mjs",
    "verify": "pnpm run fetch-kernels && pnpm run verify:purity && pnpm run verify:goldens && pnpm run verify:bessel && pnpm run verify:cosmolabe && pnpm run verify:seam"
  }
}
