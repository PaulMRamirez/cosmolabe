# Re-entry brief for Aaron (living document)

Maintained continuously; the goal is that your first hour back is a walkthrough, not archaeology.

## Provisional decisions awaiting your review
Generated by scripts/review-on-return.sh; register as of 2026-07-11 (Session 8; the commits in your paths remain the Session 4 pair; the flagged set grew by one this session, M-0011, the host-authority door, joining M-0001, M-0003, M-0005, M-0007, M-0008): M-0011 opens rule 4's 'host' authority through data adapters only, with a fidelity charter (adapters map, never compute; host provenance carried in host terms) and the authoritative-plus-exploratory side-by-side criterion; the first adapter is the MMGIS-shaped one, exercised live by the fixture host. M-0001, the spine acceptance, now Accepted (provisional) with the bake-off evidence tables attached; cosmolabe's core is the spine by the pre-stated rule (it loses neither jitter nor purity; both cores share the floating-origin defense, purity differentiates at zero violations versus five DOM couplings in bessel scene). M-0003, the renderer tier acceptance, now Accepted (provisional) on the jitter envelope evidence (0.5 device px, every gated configuration one to four orders inside it); frame time and memory complete in the W4 device pass, Cesium unexercised and its gate unchanged. M-0005, the besselian defensive claim (the one post-agreement decision so far) and neutral-scope migration timing. M-0007, the W4 device-truth addendum. M-0008, the analysis surface grammar, reviewed live in the product.

## Changes in your paths
Start from `git log cosmolabe-pre-merge..main -- <your paths>`; the tag marks the exact SHA imported from your repo. (Baseline compare links and one-line rationale for every behavioral change in core and rendering.)

Session 1B (workspace, CI, kernel logistics, 2026-07-10) changed no cosmolabe-heritage source: all of it was root plumbing (pnpm workspace and the federated `pnpm verify`, LICENSE and DCO, harness consolidation under root `.claude/`, `scripts/fetch-kernels`, CI). Three inherited items in your paths are parked for `/baseline` PRs rather than touched during your leave. First, `packages/core/src/__tests__/xyzv-parser.test.ts` and `catalog-all-cosmographia.test.ts` hardcode `/Users/aplave/code/cosmographia/...`, so they fail on every machine but yours; the root gate excludes exactly those two files by name and the honest fix is a skip-guard or a fetched fixture. Second, cosmolabe ships no eslint dependency or config, so its `npm run lint` cannot run; the root gate covers bessel's lint but not cosmolabe's until an eslint setup lands. Third, the local-time epoch parse, recorded as a named finding below. None of the three is urgent and none is mine to decide unilaterally.

Session 2 (measurement rig, 2026-07-10) also changed no cosmolabe-heritage source; the register above confirms zero new commits in your paths. What it added that touches your world: the day-zero pre-repoint baselines now live at `tests/golden/pre-merge/` (numeric fingerprints byte-identical to your committed goldens, plus fresh SwiftShader render captures, checksummed, environment recorded in `ENVIRONMENT.md`), and `TZ=America/Los_Angeles node scripts/baseline.mjs compare` proves they reproduce; this is the diff anchor for the Session 3 re-point, so any change to core or rendering from here shows up as a reviewed baseline delta with a rationale, never silently. The purity lint (rule 6) now gates `packages/core` in the root verify; your core passes with zero violations, and nothing was renamed or touched to make that true. One root-owned change affects your paths' reproducibility without touching them: scripts/fetch-kernels gained the eight Cassini text kernels (cas_v43.tf, cas00172.tsc, six instrument kernels) that your repo committed at the viewer path but the Session 1B manifest missed; without them the cassini-soi scene cannot initialize outside your machine.

Session 3 (the seam, 2026-07-10) again changed no cosmolabe-heritage source; the register confirms zero new commits in your paths, and `TZ=America/Los_Angeles node scripts/baseline.mjs compare` still passes, proving the renderer path untouched. What it built around your code: bessel's SPICE layer is extracted as the internally published `cspice-wasm` package (a thin `@bessel/spice` facade keeps every bessel-heritage import resolving), and `bessel/packages/frames` implements the two M-0002 contracts exactly as typed in docs/design/02 section 2, `StateProvider` and `FramesService`; this is the tier your core will consume at the re-point. The differential harness now runs in `pnpm verify` (`/seam`, `scripts/seam.mjs`, rig at `tests/rig/seam.rig.ts`): call-parity mode is green on GS-1 and GS-2 with every delta exactly zero (your timecraftjs wrapper and the cspice-wasm build both marshal into CSPICE_N0066 and agree bit for bit over identical kernel bytes, all five corrections), and pipeline mode is measured against the 1 m and 5 arcsec tripwires with the tables committed at `docs/validation/data/seam-call-parity.json` and `seam-pipeline.json`. Every GS-2 row (your pipeline versus the frames tier) sits inside the tripwires; GS-1 surfaced the obliquity finding below. The fetch manifest gained the GS-1 cruise fixture (010420R_SCPSE_EP1_JP83.bsp, sha256 recorded from the NAIF download); the purity lint now mechanically requires the two new packages in its audit set, and both pass clean.

Session 4 (the re-point, 2026-07-11) is the first session with substantive changes in your paths, and the register above now lists exactly two commits there, each a self-contained review unit. First, the Class B obliquity fix (commit 095714b): one constant in `packages/core/src/kinematics.ts`, detailed in the named finding below with its before and after pipeline tables and baseline diff. Two dormant duplicates of the truncated constant remain untouched and are yours to decide: `packages/core/src/frames/InertialFrame.ts` (exported but with no internal runtime consumer) and `packages/cesium-adapter/src/CoordinateTransforms.ts` (the Cesium backend path), plus test-local literals that assert to tolerances far looser than the change. Second, the re-point itself (commit 7c85dec): your core's SPICE consumption now flows through the frames tier via a drop-in adapter that implements the SpiceInstance surface over cspice-wasm; the code of SpiceTrajectory, SpiceRotation, TwoVectorFrame, CatalogLoader, and the renderer is untouched, and the diff in your paths is confined to the three injection sites (the viewer loader, the trajectory cache worker, the test harness builder), a postinstall symlink script bridging the federated trees, and one ambient declaration for Vite asset imports. Your entire 312-test suite passes unchanged over the new path, call-parity between timecraftjs and cspice-wasm is exactly zero on all four golden scenarios, and the deliberate re-baseline at `tests/golden/session-4-repoint/` carries the reviewed diff (0.163 m worst fingerprint component, 2 pixels of 786432 on one render, all of it the obliquity constant, none of it the swap). Session 5 (the compute plane, 2026-07-11) changed no cosmolabe-heritage source; the register confirms the Session 4 pair remain the only commits in your paths. What it built beside your code: `@bessel/compute`, the M-0004 compute plane, with the JobHandle protocol (streaming partials, result, cancel) and the AnalysisProduct v0 schema (the four kinds of the M-0008 grammar, each with a v0 payload shape), provenance stamped from the frames tier's kernel set hash so a product's provenance row is reproducible against the committed seam tables, and `authority: 'exploratory'` enforced by the runner with a loud refusal of any job carrying an authority property (rule 4; the host door lands with the first host data adapter, by ADR). The access and coverage engines run end to end as jobs: access streams one timeline lane per target, coverage streams its field row by row with NaN marking unresolved cells, both cooperatively cancellable. The only engine-package change is additive (an optional per-cell hook and an optional AbortSignal on GridSweepRequest); cspice-wasm gained a spiceEngineOver factory so one bindings instance backs both an engine and a frames layer. The architecture pin moved deliberately to 30 packages.

Session 6 (the grammar demo and the validation page, 2026-07-11) changed no cosmolabe-heritage source; the register confirms the Session 4 pair remain the only commits in your paths. What it built: the compute plane emits all four product kinds (the series job over the eval-series machinery, the ground-track geometry job over the sub-point path, both pinned into the API snapshot additively), and the bessel web app gained a seventh analyze tab, the M-0008 grammar demo: the four kinds in their four canonical forms, live from streamed JobHandle jobs in a dedicated compute worker, with per-target lanes (the Sun lane honestly empty), a chunk-by-chunk strip chart, an in-scene ground track, and a 288-cell Walker coverage field resolving row by row, every card carrying the progress-ring tray chip with cancel and a Computed here chip whose popover shows the product's provenance block with the worker's real kernel set hash. M-0008 moved to Accepted (provisional) with the observed Playwright run as its evidence addendum; your live walkthrough review stands. The public validation page now generates from the committed tables (scripts/validation-page.mjs), Pages is enabled with the workflow build type, the deploy is environment-gated to main (verified live end to end with the placeholder; the real page publishes on this PR's merge), and pages.yml triggers on pushes to docs/validation. One protocol lesson the demo earned, fixed where it broke: worker-hosted jobs ran as one uninterruptible microtask chain so cancel messages starved; the coverage sweep now yields a macrotask per cell and the job runner per progress step, the eval-series pattern generalized.

Session 7 (the worker substrate and the panel, 2026-07-11) changed no cosmolabe-heritage source; the register confirms the Session 4 pair remain the only commits in your paths. What it built: the demo's compute worker generalized into the package-level job substrate (`@bessel/compute/worker` hosting a ComputeEnv behind a typed wire protocol, `JobClient` speaking init, publish, run, progress, cancel), with the Session 6 cancellation lesson pinned where it broke by a conformance test (a macrotask-delivered cancel must land mid-stream, between partials) and the per-cell macrotask yield replaced by a time-budgeted cadence (yield when 12 ms of wall clock elapses), so cancellation latency and progress flushing hold at 50000-cell grids without 50000 timer round trips; the demo migrated onto the substrate (the app's worker file is now a one-line import) and gained the GS-4 lanes card (Walker access windows over a ground site), closing the W3 mechanics-completeness precision. And the panel: `@bessel/panel` implements `mount()` with the signature transcribed verbatim from docs/design/02 section 7 (M-0007), v0 implementing only the 'transfer' compute mode with 'threads' and 'iframe' throwing loudly rather than degrading silently, a HostDataAdapter through which a host supplies finished products, fallback compute (kernels, worker construction, job specs), or both, and the four product kinds rendered in 2D canonical panel forms (lanes, strip chart, ground-track map, flat field heatmap) with provenance chips, progress rings, and cooperative cancel; every product the panel computes is 'exploratory' by construction (rule 4). The evidence is a bare embed host page (`embed.html`, a second vite entry consuming the panel exactly as a third party would) and an observed Playwright smoke test in which all four kinds materialize with streamed field partials. The architecture pin moved deliberately to 31 packages; the profiles stretch was again not attempted and carries to the device-truth session.

Session 8 (the host-panel contract and the MMGIS-shaped embed, 2026-07-11) changed no cosmolabe-heritage source; the register confirms the Session 4 pair remain the only commits in your paths. What it built: PanelController gained the host sync surface (the "one small spec" of docs/design/01, kept identical for every embed host): the time cursor flows both ways in ET seconds, selection and the mounted content's time span flow out, product focus flows in, and the MMGIS deep-link parameters that survive contact with panel v0 are transcribed from the heritage ADR-0008 contract with four divergences recorded in the module doc (including the civil-to-ET anchor mapping and its leap-second limit). Rule 4's host door opened by ADR M-0011: host authority enters through data adapters only, under a fidelity charter (adapters map, never compute; host provenance carried in host terms, the dataset uuid as a host: prefixed kernel-set identity), and the first adapter maps MMGIS-shaped computed layers to geometry products. The W4 "MMGIS embed behind a flag" criterion is met on a fixture named honestly as one: an MMGIS-shaped plain-DOM host page that owns its own map and time cursor mounts the panel only behind bessel=1, renders a host-authority product beside exploratory compute on one surface (the authoritative-plus-exploratory criterion, observed by Playwright with the provenance chips distinguishing them), and exercises cursor sync, deep links both directions, selection, and focus live. M-0010 gained the observed MMGIS posture: the stock server explicitly disables COOP, COEP, and CORP in its helmet config (source inspection at commit 44814b1), so a real MMGIS host gets 'transfer' as shipped, and the CSP frame directives are environment-configurable, putting the iframe branch within configuration reach; enabling isolation on MMGIS itself is MMGIS-repo work behind the PlanDev sync. Explicitly not built: the 3D-in-MMGIS story, which routes through the PlanDev-MMGIS effort.

On timecraftjs, stated precisely so "off the state path" is not misread as "retired": the dependency remains in `packages/spice/package.json` and is still constructed in exactly three places, all deliberate. First, `@cosmolabe/spice`'s own unit suite (four test files), which tests the heritage wrapper itself. Second, five core test files that build `Spice` directly as their own fixture (`cassini-spice`, `geometry-calculator`, `spice-rotation`, `two-vector-frame`, `event-finder`); these are heritage tests of heritage modules and were left untouched per rule 2. Third, the differential harness's call-parity reference lane (`tests/rig/seam.rig.ts`), which exists precisely to keep measuring the new layer against your wrapper. No runtime path constructs it: the built viewer bundle now ships the 1.35 MB cspice.wasm and no timecraftjs asm chunk (grep of `dist/assets` is clean), where the heritage build carried the roughly 20 MB asm module. Deleting the package, its dependency, and the five test-fixture uses is a single Class B decision for your return, and this inventory is the W2 gate carryover for it.

### The adapter boundary, in one paragraph

This is the one architectural note to hold onto. `createHeritageSpice` (in `@cosmolabe/frames`, `heritage-spice.ts`) is the entire boundary between your code and the new SPICE layer: your core still speaks `SpiceInstance`, the adapter implements that surface over the frames tier, and it makes no semantic choice it did not inherit from your wrapper. Its charter doc block enumerates every choice per method (correction verbatim and never defaulted, frames verbatim, str2et strips one trailing Z while utc2et does not, planetocentric sub-point semantics, cnfine windows searched independently and concatenated, coverage windows merged ascending, getfov maxBounds default 20, and typed loud throws for the four members whose CSPICE entry points the WASM allowlist does not carry), and every row of that charter is pinned by a dedicated test in `heritage-spice.test.ts` plus value-checked against your live wrapper by the harness. The dissolution plan is recorded in the same doc block: at the packages restructure, core call sites migrate from `SpiceInstance` to explicit `StateQuery` and `FramesService` calls as Class B baseline-diffed work, the adapter shrinks method by method toward zero, and any residue that earns permanence is decided by ADR with you, not by default.

### Named finding: the heritage EclipticJ2000 conversion truncates the J2000 obliquity (Class B, landed as Session 4's first commit)

`packages/core/src/kinematics.ts` defines `OBLIQUITY_J2000_RAD` from 23.4392911 degrees; SPICE's ECLIPJ2000 uses 84381.448 arcsec, which is 23.439291111... degrees. The 1.94e-10 radian difference is invisible at planetocentric scale (sub-millimeter across the whole GS-2 table) and grows linearly with distance: 87 to 95 m for Cassini at 5.6 AU on the GS-1 cruise fixture, which puts that one pipeline row outside the 1 m tripwire. The differential harness caught it on its first run and the attribution is exact: rotating the SPICE truth by the constant difference collapses the residual to about 0.1 mm at every sweep epoch (`docs/validation/data/seam-pipeline.json` carries the finding in its description). Sequencing decided at the Session 3 pre-merge gate and executed: the one-constant fix in kinematics.ts (84381.448 arcsec exactly, the IAU 1976 value the comment always cited) landed as Session 4's first commit, before any re-point work, and this entry is your one-commit review anchor for it. The before and after seam pipeline tables are the parent commit's and the fix commit's docs/validation/data/seam-pipeline.json: GS-1 Cassini 94.9 m before, 0 m after. The fix also explained more than expected: the GS-2 sub-meter rows (Titan 0.177 m, Cassini 0.11 m) were the same constant applied to the J2000-framed trajectory legs, not caching or interpolation; after the fix every GS-2 row sits at or below 0.12 mm, which is the pipeline's true noise floor. Attached baseline diff, stated in physical units: your 312-test suite passes unchanged; the pre-merge renders reproduce at 0.000 percent pixel diff; the analytical fingerprint is byte-identical; the saturn-soi fingerprint shifts by at most 0.163 m in any component (a trajectory sample at the sweep edge), which is why the deliberate re-baseline that accompanies the re-point later in Session 4 is the honest new anchor rather than an edit to the immutable pre-merge set.

### Named finding: the committed visual goldens presuppose kernels the repository does not carry (recorded, review-on-return)

Your Layer 4 goldens at `apps/viewer/test-screenshots/__goldens__/` for cassini-soi were captured with the full multiyear Cassini SCPSE and CK set present in your working tree (the golden frames show the whole-mission trajectory fan); the committed catalog kernels cover only the SOI week, so a capture from the repository as it stands differs by 2.4 to 3.0 percent of pixels (earth-moon reproduces at 0.013 percent). To state the relationship exactly: the `tests/golden/pre-merge/` cassini-soi baselines are rendered from the fetchable kernel set and therefore deliberately differ from your committed goldens, which presuppose your full kernel set; ours are the reproducible truth anyone can recapture from the repository, yours are this recorded finding, and neither is a regression of the other. Nothing was changed: your goldens and script are untouched, and the Session 2 baselines are the honest anchor for the re-point. Options on your return: re-golden from the committed kernel subset, or extend the fetch manifest to the full set you captured with.

### Named finding: naked epoch strings parse as local time (Class B, review-on-return)

Epoch strings without a zone suffix, for example the scene `defaultTime: '2004-07-01T02:48:00'` in `packages/core/src/__tests__/_harness/scenes.ts`, go through JavaScript `Date` parsing in the cosmolabe path and therefore resolve in the machine's local timezone. Observed consequence: the committed goldens for `golden-fingerprints.test.ts` were captured on a Pacific machine, so a UTC runner computes Earth's Keplerian position about 738000 km away and the test fails; the first CI run caught exactly this. Workaround now in the gate: root `verify:cosmolabe` pins `TZ=America/Los_Angeles` for the cosmolabe test run, which reproduces the goldens deterministically on any machine without touching cosmolabe source. The real fix is a Class B change in your paths, landed with a baseline diff attached and flagged review-on-return: make epoch parsing zone-explicit (UTC) wherever catalog and test epochs are read, and regenerate the goldens under UTC. Until that lands, any new golden or baseline capture must run under the gate's pinned environment, and Session 2 records that environment alongside the baselines.

## Temporary wiring (scaffolding until the packages restructure)

Three pieces of the merged tree are deliberate scaffolding with recorded end states, so read them as intent, not architecture. First, the cross-tree symlink bridge: cosmolabe's `postinstall` runs `scripts/link-seam-packages.mjs` to symlink `@cosmolabe/frames` and `cspice-wasm` out of the bessel pnpm workspace, and the packages ship a built dist (bessel's `build:seam`, run inside its gate) with exports maps serving types and import from dist and development from source; all of it exists only because npm cannot follow pnpm workspace protocols across the federated trees, and all of it dissolves into ordinary workspace dependencies at the packages restructure. Second, the `@bessel/spice` facade: a one-file re-export keeping every bessel-heritage import of `@bessel/spice` resolving after the cspice-wasm extraction; it deletes trivially once bessel-internal imports are re-pointed, restructure-time or earlier. Third, the heritage adapter described in the boundary paragraph above, with its charter and dissolution plan recorded in `heritage-spice.ts`. Session 6 adds two more items. Fourth, the grammar demo's workbench-tab wiring (the Jobs tab, its compute worker client, and the store slice behind it): scaffolding that dissolves into the panel shell at the restructure, when mount() becomes the surface the four forms live on; Session 7 made that end state concrete (mount() exists in `@bessel/panel` and renders all four forms), so the dissolution is now a migration, not a design. Fifth, the geometry and field render welds: the demo drapes those two kinds through bessel's SolarSystemScene (setOrbits, setCoverageOverlay); the panel renders all four kinds spine-agnostic in 2D (the ground-track map and the flat heatmap), but the in-scene drapes remain welded to the bessel scene, and the render-binding interface stays the weld's executioner. None of the five should survive the restructure unexamined; each either disappears or gets an ADR.

## Carryover ledger (every open item, with its executioner)

The /gate duty reads this ledger: an item without a scheduled executioner fails the next gate by name. One precision for the W3 close (Session 7 opens with it): the W3 gate words the demo as 'grammar demo on GS-2 and GS-4 (lanes, drape, provenance chips, materialization)'; the Session 6 demo exhibits all four mechanics and uses both scenarios, but not every mechanic on every scenario: GS-2 carries the lanes, the strip chart, and the ground-track drape, GS-4 carries the field drape over the Walker set, and there is no GS-4 lanes card (Walker access windows over a ground site). The GS-4 lanes gap: absorbed in Session 7 (the Walker site passes card, observed in the Playwright run), closed. Worker-substrate job execution and cancellation hardening: executed in Session 7 (the substrate generalized into `@bessel/compute` with the mid-stream cancel pinned by conformance test and the per-cell yield replaced by the 12 ms time-budgeted cadence), closed. Geometry and series product emission: executed in Session 6 (the series and ground-track jobs), closed. Convergence of the pre-existing ad hoc invocation patterns onto JobHandle (the app's bespoke coverage worker protocol, the SDK's threaded-but-inert AbortSignal) lands at the packages restructure alongside the heritage adapter's dissolution. The panel's unimplemented compute modes throw at mount() by design, not by omission, and carry with named executioners: 'threads' lands when a workbench or device performance need demands shared memory (the capability probe of the profiles and device-truth work supplies the gate, the need supplies the trigger), and 'iframe' lands with the first embed host that cannot serve COOP and COEP headers (the M-0010 isolation posture exists for exactly that host). One sentence worth holding onto for the device pass: transfer-only v0 is by design the Companion baseline (M-0007 records that Capacitor cannot set the isolation headers on the initial document, so transferable-buffer compute is the Companion's floor), which means the device-truth session tests the panel as it ships today, not a future mode. Two entries added at the Session 7 pre-merge gate, one now closed: the spine-embedded panel (the scene-bearing surface that would evidence the section 10 scene-first heuristics of M-0008, which panel v0, scene-less by design, does not) still carries, executioner the render-binding interface work; the host-authority path is executed and closed (Session 8: M-0011 opened the door, createMmgisDataAdapter is the first adapter, and the fixture host renders a 'host' product beside 'exploratory' compute, observed by Playwright). The iframe entry's trigger is sharpened by the M-0010 observation: MMGIS's stock server cannot serve COOP and COEP as configured (helmet disables them), so MMGIS is the standing example of the host class that would eventually want 'iframe', while its environment-configurable frame directives mean permitting the iframe needs no MMGIS code change. Two orphans swept in at the Session 8 pre-merge gate, both carried until now only in the session reports rather than in this ledger: the mid-job furnish provenance guard (Session 5: provenance is stamped once at submit, so a mid-job furnish would carry a stale kernel set hash; the deferred runtime guard's trigger is re-sharpened to any adapter or host path that can furnish after submit, a path that today does not exist, and the entry is dormant, since the MMGIS adapter touches no SPICE surface); and the M-0008 color-strategy colorbar picker (Session 6, recorded as not done: the field ramp is the existing viridis overlay, not the named color strategies the grammar specifies), executioner the spine-embedded panel work, the same entry as the scene welds. Confirmed while sweeping: the live time-evolving drape (Session 6's first residual, the ground track drawn as a body-fixed snapshot rotated once at the demo epoch) is covered by that same spine-embedded panel entry, since the drape's motion is render-binding work. Standing items: timecraftjs package deletion (Class B, yours on return, inventory in the Session 4 paragraph); the profiles package, re-homed at the Session 8 pre-merge gate (no longer a floating stretch: it is written into docs/goals/SESSION-DEVICE-TRUTH.goal.md as a post-measurement step, the skeleton plus tier defaults derived from the measured numbers, and its carry count ends at four) and the frame time and memory measurements (the W4 device-truth pass, M-0007); the physical-device items (the Companion prototype on an iPhone, WKWebView SharedArrayBuffer and WebGPU probes, the M-0007 addendum), which need hardware in hand and were named rather than absorbed in Session 7; the three parked Session 1B heritage items (yours); Cesium unexercised with its earn-its-keep gate unchanged (M-0003); the W3 porkchop inspector stretch.

## Window gates

W3 (engines through the compute plane) closed 2026-07-11 by /gate, every exit criterion met on observed evidence, one with a named precision. The job protocol and AnalysisProduct v0 are live (PR #5, with the stability policy, wire encoding, and API snapshot from its pre-merge amendments); access and coverage run end to end as jobs, joined by the series and ground-track emitters (PR #6); the grammar demo runs on GS-2 and GS-4 with lanes, drape, provenance chips, and materialization, read per the recorded precision as mechanics-complete across the pair (the GS-4 lanes card is the named gap, absorbed or carried by Session 7); the validation page skeleton is deployed to Pages through the verified OIDC path (live at paulmramirez.github.io/cosmolabe since the PR #6 merge, with the merged-project framing); and M-0008 is Accepted (provisional) with the demo evidence addendum and its honest scope. The W3 stretch, the porkchop inspector, was not attempted and carries. Scaffolding duty on the five temporary-wiring items: every executioner still stands (the symlink bridge, the facade, and the adapter at the packages restructure; the demo-tab wiring and the scene render welds at the panel shell, which Session 7 begins). Remaining carryover unchanged: timecraftjs deletion (yours, Class B), profiles (twice carried, in the Session 7 goal as stretch), the device-truth pass and frame time and memory (W4, hardware in hand), the three parked Session 1B heritage items, Cesium unexercised.

W2 (one core on one SPICE layer) closed 2026-07-11 by /gate, three of four exit criteria met outright and the fourth met with a named precision. The re-point merged behind a green pipeline-mode harness on all four scenarios (PR #4, merge 907f59b, with `verify:seam --strict-pipeline` in the gate and every row at the 0.1 mm noise floor). Visual regression is green: renders reproduce even against the immutable pre-merge set (0.000 percent on two frames, 0.0003 percent on the SOI frame, far inside the 0.5 percent gate), and the numeric fingerprints are green against the reviewed `session-4-repoint` anchor, with the pre-merge divergence being exactly the recorded, reviewed obliquity diff (0.163 m worst component). M-0001 is accepted provisional since Session 2. The "timecraftjs retired" criterion is met on the runtime state path only (the built viewer ships cspice.wasm and no asm chunk) and the dependency itself remains with three named construction sites; that inventory (see the Session 4 paragraph above) is this gate's honest carryover, with package deletion a Class B decision on your return. The W2 stretch item, the `profiles` package, was not attempted and carries to the M-0007 device-truth context in W4. Remaining carryover unchanged from W1: the three parked Session 1B heritage items, frame time and memory to the W4 device pass, Cesium unexercised with its gate unchanged.

W1 (the seam is measured) closed 2026-07-10 by /gate, every exit criterion met on observed evidence: monorepo live with CI green on both imported trees (the `verify` check on merged PRs 1 through 3); seed landed (Session 1); cspice-wasm and frames extracted (PR #3, merge 64d04a4); harness green in call-parity on GS-1 and GS-2 (`docs/validation/data/seam-call-parity.json`, 38 rows, every delta exactly zero); baselines captured (`tests/golden/pre-merge/`, tree pinned); bake-off run with evidence attached to M-0001 and M-0003 (Session 2 addenda). The stretch item, GS-3 and GS-4 fixtures, was not reached and carries over by name. Honest carryover into W2 (Session 4 owns the first three): the GS-3 and GS-4 fixtures; the obliquity Class B fix, sequenced as Session 4's first commit; the GS-1 pipeline row outside the 1 m tripwire until that fix lands; frame time and memory measurements, deferred to the W4 device pass (recorded in M-0001); the three parked Session 1B heritage items (Aaron-local test paths, no cosmolabe eslint, the local-time epoch parse); Cesium unexercised with its earn-its-keep gate unchanged (M-0003).

## The demo
(Script for the live grammar walkthrough; current state of GS-2 and GS-4 scenes.)

## Open items parked for you (Class C)
Aerie adapter beyond prototype; Cesium deprecation ADR if the gate ever fails twice; public launch communications; neutral-scope migration timing.

## Suggested first session
Ninety minutes: demo, provisional ADR pass with veto, divide what remains.
