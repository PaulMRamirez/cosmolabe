# Re-entry brief for Aaron (living document)

Maintained continuously; the goal is that your first hour back is a walkthrough, not archaeology.

## Provisional decisions awaiting your review
Generated by scripts/review-on-return.sh; register as of 2026-07-10 (Session 3, unchanged from Session 2: the seam session ratified nothing new and changed no tolerance, so M-0002 stays unflagged; the flagged set remains M-0001, M-0003, M-0005, M-0007, M-0008): M-0001, the spine acceptance, now Accepted (provisional) with the bake-off evidence tables attached; cosmolabe's core is the spine by the pre-stated rule (it loses neither jitter nor purity; both cores share the floating-origin defense, purity differentiates at zero violations versus five DOM couplings in bessel scene). M-0003, the renderer tier acceptance, now Accepted (provisional) on the jitter envelope evidence (0.5 device px, every gated configuration one to four orders inside it); frame time and memory complete in the W4 device pass, Cesium unexercised and its gate unchanged. M-0005, the besselian defensive claim (the one post-agreement decision so far) and neutral-scope migration timing. M-0007, the W4 device-truth addendum. M-0008, the analysis surface grammar, reviewed live in the product.

## Changes in your paths
Start from `git log cosmolabe-pre-merge..main -- <your paths>`; the tag marks the exact SHA imported from your repo. (Baseline compare links and one-line rationale for every behavioral change in core and rendering.)

Session 1B (workspace, CI, kernel logistics, 2026-07-10) changed no cosmolabe-heritage source: all of it was root plumbing (pnpm workspace and the federated `pnpm verify`, LICENSE and DCO, harness consolidation under root `.claude/`, `scripts/fetch-kernels`, CI). Three inherited items in your paths are parked for `/baseline` PRs rather than touched during your leave. First, `packages/core/src/__tests__/xyzv-parser.test.ts` and `catalog-all-cosmographia.test.ts` hardcode `/Users/aplave/code/cosmographia/...`, so they fail on every machine but yours; the root gate excludes exactly those two files by name and the honest fix is a skip-guard or a fetched fixture. Second, cosmolabe ships no eslint dependency or config, so its `npm run lint` cannot run; the root gate covers bessel's lint but not cosmolabe's until an eslint setup lands. Third, the local-time epoch parse, recorded as a named finding below. None of the three is urgent and none is mine to decide unilaterally.

Session 2 (measurement rig, 2026-07-10) also changed no cosmolabe-heritage source; the register above confirms zero new commits in your paths. What it added that touches your world: the day-zero pre-repoint baselines now live at `tests/golden/pre-merge/` (numeric fingerprints byte-identical to your committed goldens, plus fresh SwiftShader render captures, checksummed, environment recorded in `ENVIRONMENT.md`), and `TZ=America/Los_Angeles node scripts/baseline.mjs compare` proves they reproduce; this is the diff anchor for the Session 3 re-point, so any change to core or rendering from here shows up as a reviewed baseline delta with a rationale, never silently. The purity lint (rule 6) now gates `packages/core` in the root verify; your core passes with zero violations, and nothing was renamed or touched to make that true. One root-owned change affects your paths' reproducibility without touching them: scripts/fetch-kernels gained the eight Cassini text kernels (cas_v43.tf, cas00172.tsc, six instrument kernels) that your repo committed at the viewer path but the Session 1B manifest missed; without them the cassini-soi scene cannot initialize outside your machine.

Session 3 (the seam, 2026-07-10) again changed no cosmolabe-heritage source; the register confirms zero new commits in your paths, and `TZ=America/Los_Angeles node scripts/baseline.mjs compare` still passes, proving the renderer path untouched. What it built around your code: bessel's SPICE layer is extracted as the internally published `cspice-wasm` package (a thin `@bessel/spice` facade keeps every bessel-heritage import resolving), and `bessel/packages/frames` implements the two M-0002 contracts exactly as typed in docs/design/02 section 2, `StateProvider` and `FramesService`; this is the tier your core will consume at the re-point. The differential harness now runs in `pnpm verify` (`/seam`, `scripts/seam.mjs`, rig at `tests/rig/seam.rig.ts`): call-parity mode is green on GS-1 and GS-2 with every delta exactly zero (your timecraftjs wrapper and the cspice-wasm build both marshal into CSPICE_N0066 and agree bit for bit over identical kernel bytes, all five corrections), and pipeline mode is measured against the 1 m and 5 arcsec tripwires with the tables committed at `docs/validation/data/seam-call-parity.json` and `seam-pipeline.json`. Every GS-2 row (your pipeline versus the frames tier) sits inside the tripwires; GS-1 surfaced the obliquity finding below. The fetch manifest gained the GS-1 cruise fixture (010420R_SCPSE_EP1_JP83.bsp, sha256 recorded from the NAIF download); the purity lint now mechanically requires the two new packages in its audit set, and both pass clean.

### Named finding: the heritage EclipticJ2000 conversion truncates the J2000 obliquity (Class B, lands first in Session 4)

`packages/core/src/kinematics.ts` defines `OBLIQUITY_J2000_RAD` from 23.4392911 degrees; SPICE's ECLIPJ2000 uses 84381.448 arcsec, which is 23.439291111... degrees. The 1.94e-10 radian difference is invisible at planetocentric scale (sub-millimeter across the whole GS-2 table) and grows linearly with distance: 87 to 95 m for Cassini at 5.6 AU on the GS-1 cruise fixture, which puts that one pipeline row outside the 1 m tripwire. The differential harness caught it on its first run and the attribution is exact: rotating the SPICE truth by the constant difference collapses the residual to about 0.1 mm at every sweep epoch (`docs/validation/data/seam-pipeline.json` carries the finding in its description). Nothing in your source was changed this session. Sequencing decided at the Session 3 pre-merge gate: the one-constant fix in kinematics.ts lands as its own Class B commit at the start of Session 4, before any re-point work, baseline-diffed per rule 2 and carrying the seam pipeline table before and after (the GS-1 row dropping from about 95 m to sub-millimeter is the reviewable claim); the re-point commits follow separately, behind the then-green strict pipeline gate. This entry becomes your one-commit review anchor for the constant change.

### Named finding: the committed visual goldens presuppose kernels the repository does not carry (recorded, review-on-return)

Your Layer 4 goldens at `apps/viewer/test-screenshots/__goldens__/` for cassini-soi were captured with the full multiyear Cassini SCPSE and CK set present in your working tree (the golden frames show the whole-mission trajectory fan); the committed catalog kernels cover only the SOI week, so a capture from the repository as it stands differs by 2.4 to 3.0 percent of pixels (earth-moon reproduces at 0.013 percent). To state the relationship exactly: the `tests/golden/pre-merge/` cassini-soi baselines are rendered from the fetchable kernel set and therefore deliberately differ from your committed goldens, which presuppose your full kernel set; ours are the reproducible truth anyone can recapture from the repository, yours are this recorded finding, and neither is a regression of the other. Nothing was changed: your goldens and script are untouched, and the Session 2 baselines are the honest anchor for the re-point. Options on your return: re-golden from the committed kernel subset, or extend the fetch manifest to the full set you captured with.

### Named finding: naked epoch strings parse as local time (Class B, review-on-return)

Epoch strings without a zone suffix, for example the scene `defaultTime: '2004-07-01T02:48:00'` in `packages/core/src/__tests__/_harness/scenes.ts`, go through JavaScript `Date` parsing in the cosmolabe path and therefore resolve in the machine's local timezone. Observed consequence: the committed goldens for `golden-fingerprints.test.ts` were captured on a Pacific machine, so a UTC runner computes Earth's Keplerian position about 738000 km away and the test fails; the first CI run caught exactly this. Workaround now in the gate: root `verify:cosmolabe` pins `TZ=America/Los_Angeles` for the cosmolabe test run, which reproduces the goldens deterministically on any machine without touching cosmolabe source. The real fix is a Class B change in your paths, landed with a baseline diff attached and flagged review-on-return: make epoch parsing zone-explicit (UTC) wherever catalog and test epochs are read, and regenerate the goldens under UTC. Until that lands, any new golden or baseline capture must run under the gate's pinned environment, and Session 2 records that environment alongside the baselines.

## The demo
(Script for the live grammar walkthrough; current state of GS-2 and GS-4 scenes.)

## Open items parked for you (Class C)
Aerie adapter beyond prototype; Cesium deprecation ADR if the gate ever fails twice; public launch communications; neutral-scope migration timing.

## Suggested first session
Ninety minutes: demo, provisional ADR pass with veto, divide what remains.
