# Goal: the grammar demo and the validation page (Session 6)

Session 5 (the compute plane) is complete: `@bessel/compute` carries the JobHandle protocol and the AnalysisProduct v0 schema exactly as typed in docs/design/02 section 5, the access and coverage engines run end to end as jobs with streamed partials and cooperative cancellation, provenance is stamped from the frames tier's kernel set hash with authority `'exploratory'` enforced loudly (iron rule 4), and the conformance suite pins the schema, the authority refusal, cancellation semantics, and partial order. Session 6 finishes the W3 gate: the four-form grammar made visible in the product, and the public validation page skeleton deployed.

Steps: complete the product-kind emitters the demo consumes, per the carryover ledger: a series job over the eval-series machinery (strip charts need TimeSeries products) and a geometry job from the footprint path (drapes need GeoLayer products), each conforming to the committed schema snapshot and wire encoding additively. Then build the grammar demo on the GS-2 and GS-4 scenarios per docs/design/03 (ADR M-0008): access intervals rendering as timeline lanes under the scrubber with windows drawing on as partials stream, the coverage field rendering as the heatmap drape resolving cell by cell, series and geometry products through their canonical forms, one legend chip per layer carrying the provenance state (the `'Computed here'` chip whose popover shows engine and version, kernel set hash, frame and correction, timestamp, straight from the Provenance block), and job tray chips with name, progress ring, and cancel wired to JobHandle. Where the demo host lives (the bessel web app's existing panels versus a cosmolabe viewer surface) follows the path of least heritage disturbance: bessel's web app already consumes the engines and is not under rule 2 protection; no cosmolabe-heritage source changes without a `/baseline` diff. Ratify M-0008 (provisional) with the demo as evidence and set its status line accordingly (it stays review-on-return; Aaron reviews the grammar live in the product). Assemble the validation page skeleton in docs/validation (index.html generated from the committed machine-readable tables: the seam call-parity and pipeline tables, the state-error and jitter tables, each with its gate and its reproduction command), flip `.github/workflows/pages.yml` from workflow_dispatch to push on main, and verify the OIDC deploy path.

Verified by: the demo renders both products from live jobs with streamed materialization observed (screen capture or e2e assertion, honestly reported); cancellation from the tray chip works; the provenance popover shows the real kernel set hash matching the committed seam tables; `pnpm verify` green locally and in CI; the seam harness still green in strict mode; `TZ=America/Los_Angeles BASELINE_DIR=tests/golden/session-4-repoint node scripts/baseline.mjs compare` green; the Pages deploy publishes the validation page with the four data tables and their gates readable.

Exit: run scripts/review-on-return.sh and refresh the register and brief (M-0008's status change is register material); run /verify-spec and commit the report; draft the next goal file (the W4 delivery-truth window: panel mount with fallback compute, the Companion device pass for the M-0007 addendum, per docs/design/04).
