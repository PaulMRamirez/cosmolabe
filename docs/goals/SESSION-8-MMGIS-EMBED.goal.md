# Goal: the host-panel contract, the first host data adapter, and the MMGIS embed behind a flag (Session 8)

Session 7 (the worker substrate and the panel) is complete: the job substrate lives in `@bessel/compute` with the mid-stream cancel pinned by conformance test and the time-budgeted yield cadence, the grammar demo runs on it, `@bessel/panel` implements `mount()` per docs/design/02 section 7 with fallback compute and all four kinds in 2D canonical forms, and the embed smoke test observes a bare host page materializing products. W4 (delivery truth) stays open: its remaining exit criteria are the physical-device items and the MMGIS embed, and this session executes the one that needs no hardware. The device-truth session (docs/goals/SESSION-DEVICE-TRUTH.goal.md, drafted and ready) preempts this goal the moment hardware is in hand; if it is, run that session instead and carry this file forward unchanged.

Scope, stated precisely so the session cannot drift into someone else's story: this is embed mechanics plus the first host data adapter, not the 3D-in-MMGIS integration. The "embed our engine inside MMGIS as its 3D view" thesis of docs/design/01 routes through the PlanDev-MMGIS effort and MMGIS's own repository, and that sync becomes a prerequisite only when work targets MMGIS's repo; nothing here does. What this session proves is the other direction: MMGIS-shaped data flowing into our panel.

Steps: first, the host-panel contract, the one small spec docs/design/01 says to write early and keep identical for Aerie and MMGIS: extend `PanelController` with the typed host sync surface (time cursor in and out, selection, product focus) and define the deep-link parameter contract for the MMGIS handoff (the bessel heritage ADR-0008 parameters, extracted from the actual MMGIS repository, are the prior art; transcribe what survives contact with the panel's v0 surface and record divergences). Additive only: mount() keeps its M-0007 signature, the controller gains methods, and the API stays consistent with the panel's v0 posture (loud throws over silent degradation). Second, the first host data adapter, the item the carryover ledger schedules here: a HostDataAdapter that maps MMGIS-shaped data (a computed layer with its provenance, in MMGIS's own terms) to AnalysisProduct values carrying `authority: 'host'`, exercising rule 4's host door for the first time; the door opens by ADR (M-0004 reserved exactly this), so draft that ADR with the boundary stated (host authority enters only through adapters that faithfully carry the host's own provenance, engines still emit 'exploratory', and the panel renders the two side by side, which is the authoritative-plus-exploratory criterion of the PlanDev source-of-truth layering in docs/design/01). Third, the MMGIS embed behind a flag: an MMGIS-shaped host fixture (a 2D GIS page that owns the map and the time cursor, hosting the panel through mount() and the sync contract), named honestly as a fixture rather than a live MMGIS integration; exercise the deep link both directions, show a host product and a computed product on one surface with their provenance chips distinguishing them, and gate it with a Playwright spec beside the embed smoke test. While there, record MMGIS's observed posture on the M-0010 isolation tree (whether an MMGIS deployment can serve COOP and COEP, and therefore which compute mode a real MMGIS host would get: 'transfer' as shipped, or 'iframe' when that mode earns its executioner) as a note in M-0010. Fourth, if capacity holds, the thrice-carried `profiles` package skeleton (the M-0007 tier ladder) consumed by the panel's device-class defaults; it otherwise carries to the device-truth session with its executioner unchanged.

Verified by: the host sync contract typed in `@bessel/panel` and pinned by unit test; the host-door ADR drafted with rule 4's boundary stated; a host product with `authority: 'host'` and a computed product with `authority: 'exploratory'` rendered on one mounted surface, distinguished by their provenance chips, observed by Playwright; the MMGIS-shaped fixture host mounts the panel behind a flag with cursor sync and deep links observed by Playwright; the M-0010 note recorded; `pnpm verify` green locally and in CI; strict seam green; `TZ=America/Los_Angeles BASELINE_DIR=tests/golden/session-4-repoint node scripts/baseline.mjs compare` green; the carryover ledger updated with what this session executed and what it did not.

Exit: run scripts/review-on-return.sh and refresh the register and brief; run /verify-spec and commit the report; draft the next goal file (the device-truth session if hardware is in hand, else the CCSDS round-trip stretch or the W4 gate evaluation, whichever the ledger owes first).
