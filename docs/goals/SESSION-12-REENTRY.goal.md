# Goal: the W5 re-entry package (Session 12)

Session 11 (the nightly Horizons spot-check) is complete: the external-truth lane runs observed-green with its committed table and the validation page carries it. W4 remains held open on the device items alone, and docs/goals/SESSION-DEVICE-TRUTH.goal.md preempts this goal the moment an iPhone is in hand. What remains executable is W5 itself: the design 04 window table's last row (buffer and re-entry) exits on "honest carryover written; RE-ENTRY-BRIEF current; demo script for Aaron's walkthrough rehearsed", and the brief's demo section is still the placeholder it was seeded with.

Steps: first, the post-merge observation Session 11 owes: dispatch the horizons-nightly workflow from main, observe the run, and amend the Session 11 report's workflow row from its recorded blocker to the observed conclusion (green, or the honest failure). Second, the demo script: replace the brief's placeholder demo section with the walkthrough Aaron's first hour runs on, written as a script with observable checkpoints, not prose promises: the built app's grammar tab end to end (the six cards, streamed materialization, provenance chips with the real kernel set hash, cooperative cancel), the bare embed host and the MMGIS-shaped fixture (the flag, host authority beside exploratory, cursor sync both directions), and the validation page walk (harness tables, the Horizons lane, the reports index). Each checkpoint names the command or URL and what he should see; rehearse it by executing every step against the built app and recording that the rehearsal happened with any deviations fixed or named. Third, the re-entry package, built exactly once here (the review-packets spec is folded into this goal at the Session 11 pre-merge gate; no separate packets goal exists): one review packet per flagged ADR in the register (seven today), each carrying the decision in three sentences, in-repo evidence links (the tables, tests, reports, and commits that ground it), what changed since the decision was recorded, the question Aaron is asked, and the veto shape with its cost (what reverses, what it touches, roughly what it costs to unwind); then the walkthrough agenda ordered by dependency first and descending veto cost second, with device-pending slots named as such (the M-0007 addendum and M-0001's deferred legs get agenda slots that say "pending hardware" rather than vanishing). Alongside the packets: regenerate the register, sweep the carryover ledger so every open item's executioner is current after eleven sessions, and confirm the temp-wiring inventory's five entries still name live triggers. Fourth, the W5 gate: run /gate against the W5 row and close it if the three criteria hold on observed evidence (the ledger and brief work is this session; the demo script rehearsal is its own evidence), with W4's held-open state restated rather than absorbed.

Verified by: the horizons-nightly run observed from main and the Session 11 report row amended; the demo script present in the brief with a recorded rehearsal (every checkpoint executed against the built app); the seven review packets present with evidence links that resolve and the agenda ordered as specified; the register regenerated and the ledger swept with executioners current; /gate run on W5 with its honest outcome recorded; `pnpm verify` green locally and in CI; strict seam green; `TZ=America/Los_Angeles BASELINE_DIR=tests/golden/session-4-repoint node scripts/baseline.mjs compare` green.

Exit: run scripts/review-on-return.sh and refresh the register and brief; run /verify-spec and commit the report; draft the next goal file (the device-truth session if hardware is in hand; otherwise the program is at its planned buffer, and the next goal names whatever the W5 gate's carryover says it must).
