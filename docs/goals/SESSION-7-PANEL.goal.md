# Goal: the panel and the worker substrate (Session 7)

Session 6 (the grammar demo and the validation page) is complete: the four product kinds run live in the app from streamed JobHandle jobs on GS-2 and GS-4 with visible provenance and cooperative cancel, M-0008 is Accepted (provisional) with the observed Playwright run as evidence, and the public validation page deploys from main through the verified OIDC path. Open with /gate on W3: the job protocol and AnalysisProduct v0 are live, both engines run as jobs, the grammar demo covers GS-2 and GS-4, the page is deployed, and M-0008 is ratified provisional, so every W3 criterion should read met, with the porkchop inspector stretch carried. Per the gate's scaffolding duty, re-assert the temporary-wiring executioners. Session 7 then starts the W4 delivery-truth window with the two items the carryover ledger schedules here.

Steps: first, the worker-substrate execution the ledger owes this session: generalize the demo's compute worker into the job substrate (one worker module hosting a ComputeEnv, a typed client speaking run, progress, cancel), migrate the demo onto it, and harden cancellation per the Session 6 lesson (macrotask yields at stream boundaries are now in the sweep and the runner; add a conformance test that a worker-hosted job cancels mid-stream, so the property is pinned where it broke, not just where it was fixed). Second, the panel: `mount()` per docs/design/02 section 7 (M-0007), the embeddable analysis surface that panel hosts consume, with fallback compute (the panel computes through the same job substrate when the host supplies no data adapter; `authority` stays 'exploratory' by construction, iron rule 4). Wire the grammar demo's four forms as the panel's first mounted content and an embed smoke test as the gate. Third, if capacity holds (the W2 stretch, twice carried): the `profiles` package skeleton (the tier ladder of M-0007) consumed by the panel's device-class defaults. The physical-device items of W4 (the Companion prototype on an iPhone, WKWebView SAB and WebGPU probes, the M-0007 addendum) need hardware in hand and land in their own session; name them in the gate carryover rather than absorbing them here.

Verified by: a worker-hosted job cancelled mid-stream by conformance test; the panel mounts in a bare host page with fallback compute producing all four product kinds through the substrate; the embed smoke test green in the gate; `pnpm verify` green locally and in CI; strict seam green; `TZ=America/Los_Angeles BASELINE_DIR=tests/golden/session-4-repoint node scripts/baseline.mjs compare` green; the carryover ledger updated with what this session executed and what it did not.

Exit: run scripts/review-on-return.sh and refresh the register and brief; run /verify-spec and commit the report; draft the next goal file (the device-truth session or the MMGIS embed, whichever unblocks first).
