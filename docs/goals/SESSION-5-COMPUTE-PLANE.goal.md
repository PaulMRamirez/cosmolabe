# Goal: the compute plane (Session 5)

Session 4 (the re-point) is complete: the Class B obliquity fix landed first with its before and after pipeline tables, GS-3 and GS-4 joined the harness, `verify:seam` runs strict, and cosmolabe consumes the frames tier through the heritage adapter with timecraftjs off the runtime state path; the deliberate re-baseline anchor is `tests/golden/session-4-repoint/` with the reviewed diff attached. Open with /gate on W2 (every W2 exit criterion should read met once the Session 4 PR merges: re-point behind the green strict harness, timecraftjs retired from the runtime, visual regression green against the reviewed anchor, M-0001 accepted provisional since Session 2) and write its honest carryover. Session 5 then starts the W3 critical path: every engine behind one job protocol with one small product schema, so analysis materializes identically across panel, app, SDK, and CLI (docs/design/02 section 5, ADR M-0004).

Steps: stand up the `JobHandle` protocol exactly as typed in docs/design/02 section 5 (streaming progress with partials as an AsyncIterable, a result promise, cancel) and the `AnalysisProduct` v0 schema (the four product kinds, intervals, series, geometry, field; the `Provenance` block whose `kernels.setHash` comes from the frames tier's `KernelSetInfo`, whose `frame` and `correction` are the M-0002 types, and whose `authority` field is `'exploratory'` from every engine, `'host'` only ever from host data adapters, iron rule 4); wire the access engine and the coverage engine end to end as jobs over the frames tier, emitting products with honest provenance and streaming partials; give the differential harness's kernel set hashes and the job provenance the same source so a product's provenance row is reproducible against the committed seam tables. Add conformance tests that pin the product schema, the provenance authority rule (an engine attempting `'host'` fails loudly), cancellation, and partial streaming order.

Verified by: the two engines run as jobs from a test driver with streamed partials observed and cancellation honored; every emitted product carries `authority: 'exploratory'` and a kernel set hash equal to the frames tier's for the same furnished bytes; `pnpm verify` green locally and in CI with the new packages in the purity audit set; the seam harness still green in strict mode; `TZ=America/Los_Angeles BASELINE_DIR=tests/golden/session-4-repoint node scripts/baseline.mjs compare` green, proving the renderer path untouched by compute-plane work.

Exit: run scripts/review-on-return.sh and refresh the re-entry register and brief; run /verify-spec and commit the report; draft the next goal file (the grammar demo on GS-2 and GS-4 and the validation page skeleton, the remainder of the W3 gate, with M-0008 ratification).
