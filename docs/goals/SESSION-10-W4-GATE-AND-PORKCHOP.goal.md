# Goal: the W4 gate assessment and the porkchop inspector (Session 10)

Session 9 (the CCSDS round-trip suite) is complete: fidelity is measured and stated per format, the SPICE-truth cross-check lane runs, and the host door's second adapter (OEM ingest) is charter-tested. Of W4's exit criteria, panel mount() with fallback compute and the MMGIS embed behind a flag are met on observed evidence, and the stretch (the CCSDS suite) is executed; only the physical-device items remain, and they need hardware. As ever, docs/goals/SESSION-DEVICE-TRUTH.goal.md preempts this goal the moment an iPhone is in hand.

Open with /gate on W4, run honestly: quote the window table, cite the observed evidence for the two met criteria and the executed stretch, and record the window as held open by the device items alone (hardware in hand is the trigger; nothing else blocks), with the carryover written by name. Do not close W4 by prose; the gate's own rules decide. Run the scaffolding duty (step 6) and the budget-log duty (step 7) as written.

Then the work body: the porkchop inspector, the W3 stretch now three windows standing, from M-0008's P1 pull list. The porkchop is grammatically a field: a scalar quantity (departure C3, or total delta-v) over a two-axis domain (departure epoch by arrival epoch), which is the four-form grammar's heatmap form with axes that happen to both be time. First, the emitter: a porkchopJob in @bessel/compute producing a field-kind product (the domain bounds and cell counts in the request, correction explicit, streamed row partials like the coverage sweep, cooperative cancel through the same time-budgeted yields), reusing the app's existing porkchop math (the porkchop worker's Lambert machinery) behind the JobHandle protocol rather than beside it; this also advances the ad hoc invocation convergence the ledger schedules for the restructure, and whatever of the bespoke porkchop worker protocol becomes redundant is retired or its remainder named. Mind the field contract: ScalarField's domain vocabulary is lat/lon; if a time-by-time domain does not map honestly onto it, that is a schema conversation per M-0004 (an amendment stating the generalized domain, not a silent reinterpretation), decided in the session and recorded. Second, the surface: a porkchop card in the grammar demo tab (and therefore the panel's forms if the schema allows), rendering the field with its provenance chip, streamed materialization, and cancel, observed by Playwright. The API surface changes are additive and the snapshot test moves deliberately.

Verified by: /gate run on W4 with the held-open state recorded in the brief and every duty green; porkchopJob emitting a field-kind product behind JobHandle with streamed partials and cancel, pinned by unit test; the M-0004 schema decision (fit or amendment) recorded; the inspector card observed by Playwright; `pnpm verify` green locally and in CI; strict seam green; `TZ=America/Los_Angeles BASELINE_DIR=tests/golden/session-4-repoint node scripts/baseline.mjs compare` green; the carryover ledger updated with what this session executed and what it did not.

Exit: run scripts/review-on-return.sh and refresh the register and brief; run /verify-spec and commit the report; draft the next goal file (the device-truth session if hardware is in hand, else the next executable ledger item).
