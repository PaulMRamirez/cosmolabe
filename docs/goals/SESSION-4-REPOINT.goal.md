# Goal: the re-point (Session 4)

Session 3 (the seam) is complete: cspice-wasm is extracted as the internally published CSPICE layer, the `frames` package implements both M-0002 contracts exactly as typed, and the differential harness is wired into `pnpm verify` with call-parity green on GS-1 and GS-2 (every delta exactly zero; both wrappers marshal into CSPICE_N0066). Pipeline mode is measured: every GS-2 row sits inside the 1 m and 5 arcsec tripwires, and GS-1 carries one named finding, the heritage EclipticJ2000 conversion truncates the J2000 obliquity (23.4392911 degrees in `cosmolabe/packages/core/src/kinematics.ts` versus SPICE's 84381.448 arcsec), sub-millimeter at planetocentric scale and 87 to 95 m at 5.6 AU, attributed exactly by the harness. Session 4 is the re-point: cosmolabe core consumes the frames tier, and the finding resolves by construction because frames owns frame semantics (M-0002). Iron rule 2 binds hard here: the re-point diff is mechanically minimal, and any improvement beyond what the re-point requires goes in a separate PR with a `/baseline` diff attached.

Steps: re-point cosmolabe core's SPICE consumption (SpiceTrajectory, SpiceRotation, TwoVectorFrame, CatalogLoader's coverage probe, the epoch conversions) from `@cosmolabe/spice` to the `frames` contracts, `StateProvider` and `FramesService`, keeping `correction` explicit at every re-pointed call site; the render packages keep their existing interfaces and the diff stays inside the state and orientation paths. Extend the harness with the GS-3 fixture (lunar south pole surface site: sun geometry and topocentric frames; extend the fetch-kernels manifest with the lunar kernels it needs and record their sha256s) and the GS-4 fixture (a six-plane Walker constellation in LEO, generated as a Type 13 SPK through cspice-wasm's writeSpkType13 so no external kernel is needed), and run pipeline mode across all four scenarios. Flip `verify:seam` to `--strict-pipeline` in the same PR as the re-point so the tripwires become the merge gate (M-0002: timecraftjs retires once pipeline mode is green on all four scenarios). Perform the deliberate re-baseline: capture a sibling baseline set under `tests/golden/` (never overwrite `pre-merge/`, its tree hash is pinned), attach the `/baseline` diff to the PR with deltas stated in physical units next to the M-0002 tolerances.

Verified by: call-parity still green on all scenarios; pipeline green at 1 m position and 5 arcsec pointing on GS-1 through GS-4 with `--strict-pipeline` enforcing it in `pnpm verify`; the GS-1 obliquity row inside the tripwire after the re-point, closing the Session 3 named finding; `pnpm verify` green locally and in CI; purity lint green; the re-point PR carries the reviewed baseline diff and the golden immutability check still passes on `pre-merge/`; timecraftjs no longer on cosmolabe core's runtime state path.

Exit: run scripts/review-on-return.sh and refresh the re-entry register (the re-point is the first substantive change in Aaron's paths and must read as one reviewable unit); run /verify-spec and commit the report; draft the next goal file per the W2 schedule (compute plane and AnalysisProduct v0, docs/design/02 section 5).
