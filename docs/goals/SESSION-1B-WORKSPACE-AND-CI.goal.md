# Goal: workspace, CI, and kernel logistics (Session 1B)

Genesis completed manually 2026-07-10: seed root commit, both subtrees with full history, tags pushed, LFS made self-contained after upstream was found missing an object, kernels removed from HEAD at both paths per M-0009. This session finishes Session 1. Constraint: no changes to cosmolabe-heritage source beyond what workspace mechanics require (CLAUDE.md rule 2).

Steps: wire the root workspace (pnpm-workspace.yaml spanning the bessel/ and cosmolabe/ trees as-is; no packages/ restructure yet, that follows the bake-off) with a root `verify` script running both trees' builds, lints, and tests; add root LICENSE (Apache-2.0) and a DCO file. Consolidate the harness: hoist and path-adapt /implement, /verify, /verify-spec and the settings.json allowlist from bessel/.claude/ to root .claude/, then reduce bessel/CLAUDE.md, bessel/AGENTS.md, and any cosmolabe equivalents to one-line pointers at the root CLAUDE.md; git mv bessel's pre-merge phase goal files to docs/goals/archive/. Build scripts/fetch-kernels: derive the manifest from `git lfs ls-files --long cosmolabe-pre-merge` filtered to the two removed kernel directories (the OID is the sha256; verify every download against it, including 281ecea43ba... from the public NAIF Cassini archive), download from public NAIF and PDS URLs into gitignored paths the spice tests expect, and wire it ahead of tests in verify. Port CI from the bessel pattern: pnpm verify on PRs and main, actions/checkout with lfs false, concurrency control, and a stubbed Pages OIDC deploy job for the future validation page.

Verified by: root `pnpm verify` green locally and in CI; a fresh clone with GIT_LFS_SKIP_SMUDGE=1 plus fetch-kernels passes verify (stranger-cloneability without LFS bandwidth); dash sweep clean on root docs and .claude; the previously red spice tests green via fetched kernels.

Exit: tell Paul the exact CI check name to mark required in branch protection; run scripts/review-on-return.sh and refresh the re-entry register; run /verify-spec and commit the report; draft docs/goals/SESSION-2-MEASUREMENT-RIG.goal.md (pre-repoint baseline capture from the cosmolabe subtree, purity lint on both cores, jitter scaffold, bake-off evidence for M-0001 and M-0003).
