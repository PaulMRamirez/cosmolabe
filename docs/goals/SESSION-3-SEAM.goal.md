# Goal: the seam (Session 3)

Session 2 (measurement rig) is complete: the day-zero baselines are captured and reproducible under `scripts/baseline.mjs`, the purity lint gates both cores in `pnpm verify`, the jitter scaffold and state-error rig emit their tables, and M-0001 and M-0003 carry the evidence addenda (both still review-on-return). Session 3 is the Phase 1 critical path from the merge review: make the seam real as code. Constraint unchanged: no re-point of cosmolabe-heritage source this session; the re-point happens only after the harness is green (rule 3) and lands as its own mechanically minimal session with the baseline diff attached.

Steps: extract `cspice-wasm` from bessel's spice layer as an internally published package (typed full-surface wrapper, worker pool, zero-copy batch) and stand up the `frames` package implementing the two M-0002 contracts exactly as typed in docs/design/02 section 2, `StateProvider` (batched states, `correction` explicit and required at every call site) and `FramesService` (single epoch conversion authority, inspectable frame chains, hashable kernel set); add `frames` and `cspice-wasm` to the purity-lint audit set. Build the differential harness as a root rig: both SPICE paths, cosmolabe's timecraftjs-based `@cosmolabe/spice` and the new layer, driven over GS-1 and GS-2 fixtures (GS-2 comes from the saturn-soi harness scene and its SOI kernel; GS-1 needs a Cassini cruise-segment fixture, so extend the fetch-kernels manifest with a small cruise SCPSE excerpt and record its sha256). Run call-parity mode (identical CSPICE invocations through both wrappers) and pipeline mode (through each path's caching and interpolation), emit machine-readable delta tables to docs/validation/data/, and write the `/seam` command that runs the harness and summarizes deltas against the M-0002 gates. Wire the harness into `pnpm verify` behind the same kernel fetch the gate already runs.

Verified by: call-parity green on GS-1 and GS-2 at relative 1e-12, shown by committed delta tables and a `/seam` run; pipeline mode measured and reported against the 1 m position and 5 arcsec pointing tripwires (green required before any re-point, not before this session ends); `pnpm verify` green locally and in CI with the harness wired in; purity lint green with the two new packages audited; `TZ=America/Los_Angeles node scripts/baseline.mjs compare` still green, proving the renderer path is untouched.

Exit: run scripts/review-on-return.sh and refresh the re-entry register; run /verify-spec and commit the report; draft docs/goals/SESSION-4-REPOINT.goal.md (the re-point itself: cosmolabe core consuming `frames` behind the green harness, the deliberate re-baseline against `tests/golden/pre-merge/` with the reviewed diff attached, GS-3 and GS-4 fixtures for the harness).
