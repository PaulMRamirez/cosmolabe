# Goal: measurement rig (Session 2)

Session 1 (genesis) and Session 1B (workspace, CI, kernel logistics) are complete: the monorepo is wired, the root `pnpm verify` gate is green in CI, kernels fetch and verify against sha256, and the harness is consolidated at the root. Session 2 builds the measurement rig that makes the seam work in Session 3 reviewable: the day-zero baselines, the purity lint, the jitter scaffold, and the bake-off evidence. Constraint unchanged: no improvement to cosmolabe-heritage source beyond what a step mechanically requires (CLAUDE.md rule 2); behavioral changes go in a separate `/baseline` PR.

Steps: capture the pre-repoint golden baselines from the cosmolabe subtree as it stands today, before any SPICE re-point touches the renderer, and land them under `tests/golden/pre-merge/` as immutable history (rule 5); this is a day-zero item because a missed baseline is unrecoverable, so it precedes all other Session 2 commits. Baseline capture and the jitter scaffold both run under the gate's pinned environment (`TZ=America/Los_Angeles` per the local-time epoch finding in the re-entry brief, plus the pinned node and pnpm versions); record that environment in `tests/golden/pre-merge/ENVIRONMENT.md` so any future capture reproduces byte-for-byte, and note there that the goldens are expected to shift at the Session 3 re-point as a deliberate, reviewed re-baseline, never a silent one. Wire the model-layer purity lint (rule 6) against both cores: no Svelte, React, or DOM imports in `core`, `frames`, or `engines/*`, enforced for the bessel packages and the cosmolabe `packages/core`, and add it to the gate so a violation fails `pnpm verify`. Build the jitter measurement scaffold that quantifies float32 render-origin jitter against a float64 reference at the tier boundaries M-0003 names, emitting a machine-readable table the differential harness can badge later. Run the bake-off scenes and write the evidence tables into M-0001 (spine acceptance) and M-0003 (renderer tier acceptance), replacing the "with W1 bake-off evidence" placeholders the ADRs currently carry; both ADRs are flagged `Review-on-return: yes`, so the evidence lands as an addendum Aaron reads on return, not a silent edit.

Verified by: `tests/golden/pre-merge/` populated and checksummed, with `ENVIRONMENT.md` recording the pinned capture environment and the planned Session 3 re-baseline, and a `/baseline` compare showing the captured renders reproduce; the purity lint failing on a deliberately planted DOM import in each core and passing once removed, and wired into `pnpm verify`; the jitter scaffold producing its table on the bake-off scenes with numbers inside the M-0003 tier envelopes; M-0001 and M-0003 carrying the evidence tables with the provisional-decision tags intact; root `pnpm verify` still green locally and in CI.

Exit: run scripts/review-on-return.sh and refresh the re-entry register with the new evidence in Aaron-owned paths (baseline compare links, one-line rationale per behavioral change if any); run /verify-spec and commit the report; draft docs/goals/SESSION-3-SEAM.goal.md (extract `cspice-wasm` and `frames` to the M-0002 contracts, bring the differential harness to call-parity green on GS-1 and GS-2 against the M-0002 tolerances: call-parity relative 1e-12, pipeline 1 m position and 5 arcsec pointing).
