# Validation report (page source)

This directory generates the public validation page; capability claims point here and nowhere else. Skeleton, populated from W1 onward: SGP4 conformance against SGP4-VER; HPOP force-model fixtures against GMAT with per-term error tables; Horizons cross-checks on GS-1 through GS-4; OD synthetic-truth recovery; RF closed-form checks; a live badge from the differential harness. Deployed to Pages by the same OIDC workflow as the app.
