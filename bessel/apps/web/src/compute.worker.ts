// The app's compute worker: the @bessel/compute substrate entry, bundled by
// Vite from this file so the worker URL resolves in the app's graph. All
// protocol logic lives in the package (Session 7 substrate generalization).
import '@bessel/compute/worker';
