# CLAUDE.md

This is the Bessel subtree of the merged Cosmolabe monorepo. The canonical agent
context and operating constitution now live at the repository root: read
`../CLAUDE.md`. The harness (settings and commands) is consolidated under the
root `.claude/`. This file remains only so a session or tool that opens `bessel/`
directly finds its way to the root. Bessel's own SPEC.md and docs/ stay in place
as the compute-identity reference they always were.
